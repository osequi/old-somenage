import React from "react";
import MediaQueries from "./MediaQueries";

export default {
  component: MediaQueries,
  title: "MediaQueries"
};

const Template = args => <MediaQueries {...args} />;

export const Default = Template.bind({});
Default.args = {};
