import React from "react";

/**
 * Imports other components and hooks.
 */
import Article from ".";

/**
 * Displays the Article demo.
 */
const ArticleDemo = (props) => {
  return <Article />;
};

export default ArticleDemo;
