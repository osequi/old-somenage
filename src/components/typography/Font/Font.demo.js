import React from "react";

/**
 * Imports other components and hooks.
 */
import Font from ".";

/**
 * Displays the Font demo.
 */
const FontDemo = (props) => {
  return (
    <Font name="Nimbus Sans Regular">
      Font Demo. Font family should be set.
    </Font>
  );
};

export default FontDemo;
