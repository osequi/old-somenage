import React from "react";

/**
 * Imports other components and hooks.
 */
import Cell from ".";

/**
 * Displays the Cell demo.
 */
const CellDemo = (props) => {
  return <Cell />;
};

export default CellDemo;
