import React from "react";

/**
 * Imports other components and hooks.
 */
import Aside from ".";

/**
 * Displays the Aside demo.
 */
const AsideDemo = (props) => {
  return <Aside />;
};

export default AsideDemo;
