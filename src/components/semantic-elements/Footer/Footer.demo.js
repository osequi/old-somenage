import React from "react";

/**
 * Imports other components and hooks.
 */
import Footer from ".";

/**
 * Displays the Footer demo.
 */
const FooterDemo = (props) => {
  return <Footer />;
};

export default FooterDemo;
