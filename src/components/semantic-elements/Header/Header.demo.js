import React from "react";

/**
 * Imports other components and hooks.
 */
import Header from ".";

/**
 * Displays the Header demo.
 */
const HeaderDemo = (props) => {
  return <Header />;
};

export default HeaderDemo;
