import React from "react";

/**
 * Imports other components and hooks.
 */
import Nav from ".";

/**
 * Displays the Nav demo.
 */
const NavDemo = (props) => {
  return <Nav />;
};

export default NavDemo;
