import React from "react";

/**
 * Imports other components and hooks.
 */
import TextElements from ".";

/**
 * Displays the TextElements demo.
 */
const TextElementsDemo = (props) => {
  return (
    <TextElements>
      <a href="">This link should be red.</a>
    </TextElements>
  );
};

export default TextElementsDemo;
