import React from "react";

/**
 * Imports other components and hooks.
 */
import Colors from ".";

/**
 * Displays the Colors demo.
 */
const ColorsDemo = (props) => {
  return <Colors />;
};

export default ColorsDemo;
