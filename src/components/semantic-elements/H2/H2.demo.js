import React from "react";

/**
 * Imports other components and hooks.
 */
import H2 from ".";

/**
 * Displays the H2 demo.
 */
const H2Demo = (props) => {
  return <H2 />;
};

export default H2Demo;
