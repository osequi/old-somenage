import React from "react";

/**
 * Imports other components and hooks.
 */
import Section from ".";

/**
 * Displays the Section demo.
 */
const SectionDemo = (props) => {
  return <Section />;
};

export default SectionDemo;
